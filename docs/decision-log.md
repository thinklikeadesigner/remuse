# Decision Log

| Time | Decision | Owner | Notes |
| --- | --- | --- | --- |
| TBD | Initial orchestration model created. | sprint-orchestrator | One orchestrator, parallel development/testing lanes, and dedicated review lanes. |
| 2026-05-09 | Phase 0 OpenDAW spike completed. | master-orchestrator | Use `@opendaw/studio-core`, `@opendaw/studio-adapters`, `@opendaw/studio-boxes`, and `@opendaw/lib-midi` behind `OpenDawProvider`; keep runtime proof as next OpenDAW task. |
| 2026-05-09 | External provider contract normalized. | master-orchestrator | Use async job contracts with signed artifact URLs, checksums, idempotency keys, and structured errors for de-reverb, stem separation, and MIDI conversion. |
| 2026-05-09 | Audio format policy changed to WAV. | master-orchestrator | Use WAV PCM 16-bit, 44.1 kHz as the working/provider/final format, while accepting 24-bit WAV inputs when provided. |
| 2026-05-09 | Phase 1 backend skeleton uses local file persistence. | workflow-engine-dev | Store raw canonical WAV inputs under `var/remuse/artifacts`, job records under `var/remuse/jobs`, and keep provider execution behind `PipelineProviders` for easy mock-to-real replacement. |
| 2026-05-09 | Select MVSEP as first de-reverb and stem-separation provider. | master-orchestrator | Build Phase 2 around MVSEP `sep_type=22` for de-reverb and MVSEP instrument-stem separation, with AudioShake as the production-grade stem backup and Neural Analog as the de-reverb backup. |
| 2026-05-09 | MVSEP provider lane is opt-in behind `REMUSE_PROVIDER=mvsep`. | audio-provider-dev | Keep mocks as the default local path; real MVSEP mode requires `MVSEP_API_TOKEN`, persists downloaded provider artifacts, and requests MVSEP WAV 16-bit output as the working format. |
| 2026-05-09 | MVSEP de-reverb uses local residual fallback. | audio-provider-dev | Live MVSEP testing returned dry/no-reverb without a native reverb-only artifact, so Remuse renders `reverbOnly` as `original - dryOnly` and persists it with providerNative=false. |
| 2026-05-09 | MVSEP de-reverb model fixed to FoxJoy MDX23C. | audio-provider-dev | Use `sep_type=22`, `add_opt1=0`, and `add_opt2=1` for Reverb Removal (noreverb) instead of the MVSEP default anvuew BSRoformer model. |
| 2026-05-09 | Phase 3 removes AI instrument analysis. | ai-instrument-dev | Trust MVSEP stem labels and filename suffixes as the primary instrument signal, normalize them into the ReMuse taxonomy, and route non-specific labels such as `other` and `instrumental` to human review. |
| 2026-05-09 | MVSEP instrument separation uses Ensemble All-In. | audio-provider-dev | Historical choice: `sep_type=30`, `add_opt1=0`, and `add_opt2=11` for `Ensemble All-In` with `SDR average: 13.67 (v. 2025.06.30)`. |
| 2026-05-10 | MVSEP instrument separation switched to BS Roformer SW. | audio-provider-dev | Use `sep_type=63` with no algorithm-specific `add_opt` fields for `BS Roformer SW`, and expect at most seven returned stems: vocals, optional instrumental, bass, drums, guitar, piano, and other. |
| 2026-05-09 | Phase 4 MIDI conversion uses a provider-neutral HTTP adapter. | workflow-engine-dev | Keep mock MIDI conversion as the default, add `REMUSE_MIDI_PROVIDER=http` for external conversion, preserve normalized instrument labels in requested filenames and persisted MIDI metadata, and require provider-readable artifact URLs for true remote execution. |
| 2026-05-09 | Select Spotify Basic Pitch as the first real MIDI provider. | workflow-engine-dev | Add `REMUSE_MIDI_PROVIDER=basic-pitch` to run the local Basic Pitch CLI per downloaded instrument stem, preserve ReMuse-normalized instrument labels in MIDI filenames and metadata, and treat drum/percussion output as approximate until a drum-specific provider is added. |
| 2026-05-09 | Phase 5 OpenDAW assembly uses a local-session provider. | opendaw-integration-dev | Add `REMUSE_OPENDAW_PROVIDER=local-session` as the job-server default to persist reproducible `.opendaw.json` session plans, load mapped sample libraries per MIDI track, and write a valid 16-bit/44.1 kHz stereo preview bounce while SDK-backed headless rendering remains behind `OpenDawProvider`. |
| 2026-05-09 | Add FluidSynth as the functioning final render backend. | opendaw-integration-dev | Keep OpenDAW session assembly in place, but allow `REMUSE_OPENDAW_RENDERER=fluidsynth` with a configured `.sf2` SoundFont to merge MIDI tracks, inject mapped program changes, run FluidSynth, and persist the returned 16-bit/44.1 kHz stereo WAV bounce. |
